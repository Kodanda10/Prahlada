# ?? CRITICAL: READ BEFORE ANY CHANGES ??

## ?? ABSOLUTE FILE PROTECTION PROTOCOL

**NO AGENT OR DEVELOPER MAY DELETE ANY FILES FROM THIS REPOSITORY**

### ?? FORBIDDEN ACTIONS:
- ? `git clean -fd` or any git clean commands
- ? `rm -rf` or recursive deletions
- ? Moving files to trash/recycle bin
- ? Bulk file operations without explicit approval

### ? SAFE OPERATIONS ONLY:
- ? `git add .` to stage files
- ? `git commit -m "message"` to commit changes
- ? `git push origin main` to push to remote
- ? Creating new files in appropriate directories
- ? Editing existing files with clear purpose

### ??? PROTECTION MEASURES:
1. **Always commit before making bulk changes**
2. **Use `git status` to verify file state**
3. **Never run cleanup commands on uncommitted work**
4. **Backup critical files before major operations**

### ?? Emergency Recovery:
If files are accidentally deleted:
1. Check `~/.Trash/` (macOS) or recycle bin
2. Look for `.tar.gz` or `.zip` backups in `/Users/abhijita/Documents/`
3. Contact project maintainer immediately

### ?? Project Status:
- **Test Suite**: 73 files required (currently incomplete)
- **Deployment**: Ready when all tests pass
- **Backup**: Maintain regular backups before changes

---

**VIOLATION CONSEQUENCES**: Immediate project halt and recovery required.

**Last Updated**: 2025-11-20
**Maintainer**: Abhijita